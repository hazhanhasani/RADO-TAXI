name: Android Driver APK

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - 'apps/driver/**'
      - 'scripts/**'
      - '.github/workflows/android-driver.yml'

permissions:
  contents: read
  id-token: write

concurrency:
  group: rado-driver-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Fetch permanent signing credentials from cPanel
        id: credentials
        continue-on-error: true
        run: bash scripts/fetch_cpanel_ci_credentials.sh

      - name: cPanel bootstrap notice
        if: steps.credentials.outcome != 'success'
        run: echo 'RADO cPanel secure bootstrap is not active yet; signed Driver build was skipped.'

      - name: Generate Android platform files
        if: steps.credentials.outcome == 'success'
        working-directory: apps/driver
        run: |
          flutter create . --platforms=android --project-name=rado_driver --org=ir.rado.taxi
          rm -f test/widget_test.dart
          python3 ../../scripts/configure_android_branding.py android/app/src/main/AndroidManifest.xml "RADO Driver"

      - name: Configure Android permissions
        if: steps.credentials.outcome == 'success'
        working-directory: apps/driver
        run: |
          python3 - <<'PY'
          from pathlib import Path
          path=Path('android/app/src/main/AndroidManifest.xml')
          text=path.read_text()
          marker='<manifest xmlns:android="http://schemas.android.com/apk/res/android">'
          permissions='''<manifest xmlns:android="http://schemas.android.com/apk/res/android">\n    <uses-permission android:name="android.permission.INTERNET"/>\n    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>\n    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>'''
          if 'android.permission.INTERNET' not in text: text=text.replace(marker,permissions,1)
          path.write_text(text)
          PY

      - name: Fetch official RADO driver icon
        if: steps.credentials.outcome == 'success'
        working-directory: apps/driver
        run: |
          mkdir -p assets/branding
          curl -fsSL https://rado-taxi.sbs/assets/branding/rado-driver.png -o assets/branding/rado-driver.png
          test -s assets/branding/rado-driver.png

      - name: Install dependencies and apply icon
        if: steps.credentials.outcome == 'success'
        working-directory: apps/driver
        run: |
          flutter pub get
          dart run flutter_launcher_icons

      - name: Configure permanent release signing
        if: steps.credentials.outcome == 'success'
        working-directory: apps/driver
        run: |
          echo "$KEYSTORE_B64" | base64 --decode > android/app/rado-release.keystore
          cat > android/key.properties <<EOF
          storePassword=$STORE_PASSWORD
          keyPassword=$KEY_PASSWORD
          keyAlias=$KEY_ALIAS
          storeFile=rado-release.keystore
          EOF
          python3 ../../scripts/configure_android_signing.py android/app/build.gradle.kts

      - name: Analyze
        if: steps.credentials.outcome == 'success'
        working-directory: apps/driver
        run: flutter analyze --no-fatal-infos --no-fatal-warnings

      - name: Build permanently signed release APK
        if: steps.credentials.outcome == 'success'
        working-directory: apps/driver
        run: |
          flutter build apk --release \
            --dart-define=RADO_API_BASE_URL="https://rado-taxi.sbs"

      - name: Rename APK
        if: steps.credentials.outcome == 'success'
        run: cp apps/driver/build/app/outputs/flutter-apk/app-release.apk "RADO-Driver-main-${GITHUB_RUN_NUMBER}.apk"

      - name: Upload APK artifact
        if: steps.credentials.outcome == 'success'
        uses: actions/upload-artifact@v4
        with:
          name: RADO-Driver-main-${{ github.run_number }}
          path: RADO-Driver-main-${{ github.run_number }}.apk
          if-no-files-found: error
          retention-days: 14
